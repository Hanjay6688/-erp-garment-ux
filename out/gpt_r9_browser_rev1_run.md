# Round9 browser revision1 — native run 36123487209 (frozen head d1bc8ad)

Workflow https://github.com/Hanjay6688/-erp-garment-ux/actions/runs/36123487209, trigger e5d7cf1fff68c069c3a48802d8ae7e8c57ae509c, four isolated disposable clones. Scenario and dependency hashes are in `audit/scenarios/r9_browser/MANIFEST.json`. All jobs record `primary_unchanged=true`, `clone_remaining=0`, restored Auth user count, and database shutdown. AUDITOR_SCENARIO does not authorize release or CP6 acceptance.

| Job | GitHub / native | Per-case observation | Interpretation |
|---|---|---|---|
| 108034182736 QC | SUCCESS / RUN_COMPLETE | `G8UI:UNKNOWN:QC_HEALTHY_CONTROL:rev6` PASS, real Auth/UI 10; `G8UI:UNKNOWN:QC_INITIAL_READ:rev6` COUNTEREXAMPLE according to wrapper, but raw KPI `—`, note `belum diketahui · data belum termuat`, actual HTTP 200 and refetch 10 | **False counterexample caused by auditor wrapper.** It reads `observed.primary` while frozen rev6 emits `observed.initial_kpis[0]`. A new run must adjudicate status. |
| 108034183147 Laundry | SUCCESS / RUN_COMPLETE | `G8UI:UNKNOWN:LAUNDRY_HEALTHY_CONTROL:rev5` PASS, Auth/UI 20; `G8UI:UNKNOWN:LAUNDRY_INITIAL_READ:rev5` COUNTEREXAMPLE according to wrapper; raw KPI `—`, note `belum diketahui`, actual refetch 20 | **False counterexample from same wrapper bug** (`observed.initial_kpis[1]`). New run required. |
| 108034182973 W11 parser | SUCCESS / RUN_COMPLETE | `G9UI:W11:PARSER_ERROR_VS_NETWORK` PASS; Auth RPC HTTP 200 contains inherited invalid cp3 UUID; one aborted request shows “Layanan UAT belum dapat dihubungi”, real HTTP 200 refetch/parser shows “ID Mandor bukan UUID valid.” | Valid narrow W11 control; deliberately malformed seed is test input, not product mutation. |
| 108034183039 W10 recovery | FAILURE / INCOMPLETE | `G8UI:RECOVERY:PATTERN_COMMIT_REPLY_LOST` and `G8UI:RECOVERY:ROLE_DUPLICATE_COMMIT_REPLY_LOST` both INCOMPLETE. Strict Playwright selector `.pattern-error`/`.access-error` each resolves to both `role=alert` and new `role=status`. | Scenario selector stopped before replay. No product verdict; refine status locator and click explicit retry button. |

W13 oracle M:3825 requires UNKNOWN instead of numerical zero, real positive control, and actual recovered display. W10 oracle M:1679/M:3819 requires exact retained UUID/payload after a committed reply is dropped and one final row after retry. Preserve rev1 outcomes verbatim; the next scenario revision will repair only auditor selectors and interpretation and receive distinct hashes/run IDs. Global CP6 remains HOLD, audit_complete=false, production_go=false.
