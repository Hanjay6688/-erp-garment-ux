# Initial import 51-draft selector reconstruction

Target: candidate `9add57ea8c2b6b2dc37c0134717d4d39ba30b5dc`. This is a **new source-based reconstruction after the external review**, not the lost original case or a native run. The new scenario SHA and execution result are pending. It is separate from the BS Resolution 101-source selector.

Source inspection: `erp.get_initial_import_workspace_v1(p_batch_id uuid default null)` returns full `batch` when a UUID is given, then always returns `recent` from `erp.migration_batches order by created_at desc,id limit 50`. Its public facade takes that UUID and the candidate UI sends it only from a local `batchId` state. The UI's only batch picker is `workspace.recent`; it has no search, pagination, or arbitrary-ID input. A 51st older editable DRAFT can disappear from that select. **Important narrowing:** this is a UI discovery/selection omission, not a server-side inability to retrieve a known old UUID: `public.erp_get_initial_import_workspace_v1(old_id)` supports direct by-ID access and should be tested as a positive control. The CREATE action is available via `public.erp_save_initial_import_action_v1('CREATE', payload, uuid)`, with nonfuture cutover date and a distinct code. The existing `cp6_ao_ap_installed.api.call/read` helper executes these public functions as seeded `authenticated` operator and restores admin session afterward; no direct table insert is necessary.

Proposed case: create 51 distinct DRAFTs through `api.call`, capturing UUIDs and asserting status, action, request ID and operator authorization. Because `created_at` can tie inside a transaction, the scenario determines its oldest authored row by actual `(created_at desc,id)` order. Pre-existing migration batches may interleave: an independent DB query computes the global top 50, which must match `recent`; with 51 authored rows, at least one authored row necessarily falls outside that top 50 regardless of interleaving. The test records interleaving rather than changing timestamps or refusing a valid observation.

The candidate source cap proves the potential omission. A native test must report the 51 authored IDs, status/order in DB, precise `recent` membership/count, positive newest listed, old accessible by explicit UUID, and no UI query/paging path. It must not claim end-to-end browser evidence from a native workspace call. The outer auditor runner owns rollback; no commit or cleanup via direct DML is appropriate inside this case.

## Reconstructed scenario artifact

Created `audit/scenarios/import_selector_reconstructed.py` with a literal one-entry `PLANNED_CASE_IDS` tuple and pure `cases(cur,today)` registration. The callable resolves the actual seeded OWNER/ADMIN mapping and role permissions; uses `api.call` to create 51 distinct DRAFT batches via the public facade with unique request IDs; verifies the 51 source rows in the database and their actual `(created_at desc,id)` order; compares `recent` with an independently queried global top 50; requires a listed authored positive control and reads the omitted oldest directly by known UUID. It records all authored/recent IDs and any baseline interleaving, rolls back the whole fixture to its own savepoint, then checks zero draft residue. All unexpected setup/product refusals become `INCOMPLETE`. Counterexample scope is the UI's data-source discovery, never a claim that the backend prohibits access to a known ID.

No database call, workflow dispatch, or credential access occurred during reconstruction. This file has **NOT_RUN** status. Source syntax was parsed without importing it; SHA256 is `66d0524c7665a5a68b55f3dbf6934bc5742395ed5832ffbd2301cd7d70d5e6ac` (7,670 bytes). The scenario relies on the existing disposable runner's `cp6_aw_probe.api` import origin and `api.base.OPERATOR_AUTH` fixture; verify those exact imports in the combined runner before citing a native result.
