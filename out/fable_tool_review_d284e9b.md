# Review alat writer 81fef32/6140edb/d284e9b (Audit A/Fable) — INDEPENDENT_SOURCE_REVIEW

Produk acuan: 9add57e. Head alat yang dijalankan auditor: d284e9b (diff `supabase/migrations supabase/dev supabase/release/cp6-t3 src` terhadap 9add57e KOSONG; diverifikasi lokal). Perubahan hanya: workflow auditor-scenario (+modes), `cp6_auditor_modes.py`, `cp6_auditor_scenario.py`, sample, `cp6_t3_rollback.py` + `cp6-t3-rollback.yml`, 4 rollback AW..AZ + ROLLBACKS.json, 2 berkas evidence.

## cp6_auditor_modes.py (races / http)
- Race: salinan DB baru per race (`createdb -T cp6_rollback`), setup di-commit: seed bila kosong, periode terbuka 2026-08-31, dan **grant test-only `usage on schema erp to authenticated`** (dibutuhkan fixture privat writer). Catatan: grant ini menyimpang dari ACL produksi; oracle auditor untuk facade publik tidak bergantung padanya, tetapi race yang memanggil fungsi `erp.*` langsung (bukan `public.*`) berjalan dengan ACL yang lebih longgar dari produksi. `two_sessions` = holder (transaksi terbuka) + worker (koneksi sendiri, commit sendiri), kontensi diukur via `pg_blocking_pids`; tidak menyentuh status kasus.
- HTTP: salinan DB tanpa grant (hanya facade publik, seperti produksi); PostgREST kontainer dengan image/env stack, DB diganti ke salinan; `login(role)` membuat user di Auth nyata (admin API) + baris `erp.app_users` langsung (fixture, bukan jalur provisioning produk), sign-in password → token; `anon_rpc` memakai anon key; **tidak ada JWT yang ditandatangani runtime**. Cleanup: hapus user Auth, kontainer, DB; hitungan auth.* primer harus kembali sama (jika tidak → INCOMPLETE). Kunci/token di-mask.
- Runner: status RUN_COMPLETE hanya bila cases+races+http semua lengkap; hasil per kasus tetap dicetak per baris JSON; tidak ada perubahan pada status kasus. Celah F1-04 (id ganda menimpa, status di luar kosakata) TIDAK diperbaiki di head ini.
- Verdict review: tidak ada pelonggaran oracle; batas: grant test-only di mode race; app_users disisipkan langsung di mode HTTP; browser→HTTP→runtime tidak tercakup (tidak ada UI).

## cp6_t3_rollback.py (+ 4 rollback AW..AZ)
- Build: rollback dibangun dari berkas rilis + capture katalog; hanya perubahan non-fungsi yang terdaftar (`RESTORABLE`: view payroll + check constraint AX) yang boleh dipulihkan, selain itu build berhenti. Guard: admission tertutup+drained, baris ledger sendiri dengan sha256 statements, tanpa successor, katalog terpasang dipin, kapsul historis dicek, data+ledger tak berubah sejak install (pre-use only).
- Cycle (run writer 36063754595, 22 PASS): siklus 1 membandingkan seluruh baris erp secara penuh (strict) — memenuhi "tepat"; siklus 2 AZ/AY/AX dan semua reinstall memakai `stable` (kapsul minus `captured_at` dan `boundary_snapshot`) karena reinstall menulis kapsul baru; AW→AV tetap strict. Fixture post-use = satu INSERT `erp.audit_logs` ter-commit → penolakan `AZ_POST_USE_ROLLBACK_REFUSED` dengan `changed={}`; belum matriks transaksi bisnis. AC..AV varian rilis NOT_BUILT (guard digest AC rilis tetap terbuka; F1-05/CP6-12 tetap HOLD).
- Auditor tidak mengulang capture/cycle (kesepakatan owner); hasil writer = REUSED_WRITER_EVIDENCE.

## Skenario native15 (GPT) — review oracle sebelum dispatch
- 8 sumber beku (SI-01..04, MONEY×4) + 7 rekonstruksi (ADVANCE×6, SELECTOR-51): oracle dari M:359-379, M:629-646, M:1022, M:1691, M:3816-3826; penolakan tak terduga = INCOMPLETE; tidak ada relabel; replay UUID memakai teks persis yang sudah terbukti dipancarkan produk. Tidak ditemukan pelonggaran. Batas yang diakui manifest tetap berlaku (SI-01 oracle produk opsional; MONEY DOWN pembulatan kanonik; ADVANCE prefix = inferensi; SELECTOR = discovery data UI).
- Dispatch Audit A: run 36065350201 (head alat d284e9b, sha skenario cec2ad52… cocok manifest).
