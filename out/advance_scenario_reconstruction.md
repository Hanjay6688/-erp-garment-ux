# Six-case advance scenario reconstruction

## Recovered prior source and contract

Read `recovered/CP6_AUDIT_CONTINUATION_CHECKPOINT/continuation/business/business_scenarios.py`, a prior native-NOT_RUN independent scenario. It holds six `BC-ADVANCE-{SUPPLIER,CUSTOMER,VENDOR}-{ORDERED_CONTROL,DATED_CAPACITY}` cases and four COUNT transfer/adjustment cases. This work reconstructs only the former six in a new versioned file; the lost final hash `44b075c7…` does not describe these new bytes. Older source hash reported `7720ced7…`, to be checked locally. It uses `api.call`/`api.upload`/`api.invoke` public initial-import RPC for a distinct batch, party, account, cash account, bank opening 100 and advance original 100 − previously settled 32.75 = 67.25; correction to 100 and refund 100 with dates ordered or backdated.

Master M:629-646 describes `OPENING_ADVANCE` fields, supplier/vendor debit asset and customer credit liability, original/settled/opening 100/32.75/67.25, no second historic cash posting, exact control totals, owner/admin correction/refund via existing import RPC, UUID/revision, immutable source, and refund cash direction. Master M:3816-3820 requires immutable posted facts, atomic refusal, exact idempotency, distinct economic/transaction dates, and no harmful dated qty/value prefix. The text does not prescribe one exact product refusal message or SQLSTATE for a capacity failure. Accordingly, the DATED_CAPACITY case must mark any refusal INCOMPLETE rather than PASS on arbitrary errors; successful posting with a negative historical prefix is COUNTEREXAMPLE. The ordered control can PASS only on exact observed balances/events/replay/immutability. COUNT cases are explicitly EXCLUDED.


## Candidate helper route and legal fixture check

At candidate 9add57e, `scripts/cp6_ao_ap_installed.py` exposes `call(cur, action, payload, key=None)` through `public.erp_save_initial_import_action_v1` after `ordinary(cur)` sets authenticated session and JWT, `upload` wraps SAVE_FILE, `invoke` refreshes expected batch revision through `read` and calls the same public facade. No new RPC or schema grant is needed in the reconstructed script. The recovered fixture's `api.production.prior.set_open_period` resolves through `cp6_v2620al_import_review` → `cp6_v2620al_review` → production helper; use it only as a transaction-local disposable open-period fixture and verify control date before posting. The prior file's SHA-256 independently verified locally as `7720ced7417375ba766e4552dae03804673ae080d3377d7a3835f1822b9e93d0`.

Fixture risk: all six require the runner's ordinary authenticated role to have schema access and a seeded OWNER app user, as the existing main scenario group normally sets transaction-locally. This reconstructed script must fail INCOMPLETE if the actor or fixture is unavailable, not add grants or bypass permissions. Native results on this exact path remain NOT_RUN.

## New scenario authored

Created `audit/scenarios/business_scenarios_reconstructed.py`, version `BCR1`, with literal `PLANNED_CASE_IDS` for six advance cases only. `cases(cur,today)` is registration-pure: it creates six zero-argument callables and does not touch DB until execution. It documents old checkpoint SHA and explicitly disclaims recovered-final identity/native execution. No COUNT function or case is carried over. Fixture uses the same public initial-import RPC route and unique accounts; opening control is verified (67.25 advance, 100 bank). Ordered branch compares exact dated balances, two event records, three account journal dates, immutable source, and UUID replay/event count. Backdated branch calls refund after a later correction date and checks for negative as-of capacity; any SQL refusal is captured as INCOMPLETE with SQLSTATE and atomic event observation, because no exact refusal text is contract-defined. A successful dated negative prefix is COUNTEREXAMPLE only with supporting event/date/source/replay observations; other ambiguous outcomes are INCOMPLETE. Outer fixture/setup exceptions are INCOMPLETE. No runner/DB/side connection/commit/product mutation is included.

## Static validation and status

AST parsed and compiled (without importing helper modules or opening DB); six literal planned IDs are unique and all ADVANCE, and the `cases` function AST contains no `execute`/public RPC call. New file size 12,181 bytes; SHA-256 `7b043b83dcfa9ca9ab79d13be27f3d0f881bb4ab04084ac362f7c0de5a3e1343`. This is a new audit scenario, not the lost final hash. **Native NOT_RUN**; source transport/fixture table/event shape remain runtime-dependent. Any fixture or permission issue must be INCOMPLETE. No business PASS is asserted by this static validation.

## Pre-freeze review correction

Candidate `scripts/cp6_initial_import_prepayments.py` creates `erp.initial_import_prepayments` with factual identity/source fields, amounts and `created_at`; it defines an immutable-source trigger and records corrections/refunds in separate events. No legitimate cache or `updated_at` mutation was found on that source table in this candidate. Nonetheless, `source()` now compares an explicit tuple of stable factual fields (identity, party, account, document/cutover dates, original/settled/opening amounts), excluding `created_at` and any future derived fields. Removed `NOT_RUN` from the result's runtime scope string; static NOT_RUN remains solely in the file header and this pre-dispatch note. Actor, permission, helper, fixture and unidentified SQL refusals remain INCOMPLETE. Final static AST compile and pure registration checks pass. **Superseding new file SHA-256:** `90625484eded3a0a68e8852f9c19e9e9286887c1d905821abc89931b42029618` (12,443 bytes); prior hash in preceding note is obsolete.
