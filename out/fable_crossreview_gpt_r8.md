# Audit silang Fable atas pekerjaan GPT putaran 8 — mulai 25 Sep 2026 ~05:45 UTC (diperbarui bertahap)

Cara kerja: setiap commit GPT di `audit/cp6-final-20260924-gpt-a0bcadf` dibaca; klaim run diverifikasi dari log Actions (bukan dari JSON GPT); oracle GPT dibandingkan dengan oracle Fable. Label: CONFIRMED / REFUTED / PARTIAL / NOTED.

## Putaran 1 (commit GPT 1443f16, f28a871, 44a23d8, 3027609)
| Klaim GPT | Verifikasi Fable | Label |
|---|---|---|
| Workflow audit sendiri `gpt-cp6-round8.yml` (push, jalur `audit/scenarios/c0_round8/**` dan `round8/**`), checkout writer ca7f095 / auditor 9dd7bc2 / harness c6b0e4f / candidate 25fa473 / base 1bdca37; Supabase lokal sekali pakai | File dibaca: sama dengan rantai `cp6-auditor-scenario.yml`, pin tool head 9dd7bc2 eksplisit; tidak menulis ke branch kandidat, hosted, atau produksi. Boleh dipakai sebagai INDEPENDENT_NATIVE_RERUN (label AUDITOR_SCENARIO) | CONFIRMED |
| Run 36098555186 / job 107955933618 (audit 7afd3d6): oracle C0 untuk 25 kasus T2 → 24 PASS + 1 INCOMPLETE, `primary_unchanged=true` | Log dibaca langsung: sha skenario 7c2c19b6…, run_identity tool_head 9dd7bc2 / product_ref a095a9d, 25 ID unik, 24 PASS, `G8C0:AS:ADJUSTMENT_DATE:False` INCOMPLETE `permission denied for schema erp` (skenario GPT memanggil `erp.get_owner_financial_snapshot_v2` langsung setelah helper inverse mengubah grant — batas transport skenario, bukan produk) | CONFIRMED |
| Oracle C0 GPT vs oracle Fable (`out/fable_t2_oracles_post_addendum.md`) | Sama pada intinya: kaki invoice/AP di E; kaki WIP/FG/COGS di hari barang (max(E, perpindahan)); penyesuaian di max(E, A); periode tertutup E/E/hari ini. Koreksi GPT atas draft Fable diterima: (1) bahan mentah 0 hanya SETELAH konsumsi, oracle harus mengamati juga posisi di E (Fable menulis "selalu 0"); (2) WIP tahap-1 = 82,37 persis (164,75 − 49,43 − 32,95), bukan 82,37/82,38; (3) tanggal ekonomi fakta turunan mengikuti D01.2/3, bukan dipaksa E. Dua auditor independen → 24/25 PASS di bawah C0; ADJUSTMENT_DATE keduanya INCOMPLETE (Fable: rumus harness; GPT: transport) | CONFIRMED (konvergen) |
| Run 36097284096 (audit f28a871): 33 kasus bisnis 28 PASS / 4 COUNTEREXAMPLE (MULTI_CENT ×4) / 1 INCOMPLETE (XA6 exposed prepare); races 2 PASS; HTTP H1 CE (dummy), H2/H3 PASS; selftest SELFTEST_PASS | Job list dibaca via API (107952094985 failure = ada CE/INCOMPLETE; 107952095105 selftest success; 107952095197 sentinel success). Rincian kasus diambil dari JSON GPT `out/gpt_round8_run_36097284096.json` (log 133 KB tidak dibaca ulang seluruhnya oleh Fable) | PARTIAL (bisnis: dari JSON GPT; identitas run: dari API) |
| **R8-B1-01 (tool, baru)**: mode race dan HTTP tidak memakai grup ketat B1 — status di luar kosakata diterima dan ID ganda menimpa hasil sebelumnya; job tetap RUN_COMPLETE | Log job 107952095197 dibaca langsung: `G8_TOOL:RACE_UNKNOWN` dan `HTTP_UNKNOWN` berstatus `NOT_A_VALID_STATUS` tetap dihitung, `RACE_DUP`/`HTTP_DUP` muncul dua kali (INCOMPLETE lalu PASS), job hijau. Sumber `scripts/cp6_auditor_modes.py`: `run_races`/`run_http` menyimpan hasil ke dict (`report['races'][key]`, timpa) dan `finish()` (baris 99–103) hanya menghitung INCOMPLETE tanpa cek kosakata; `runner.strict_group` hanya dipakai untuk `cases` di `cp6_auditor_scenario.py`. Klaim writer B1 ("status di luar kosakata jadi INCOMPLETE; ID ganda ditolak") **hanya benar untuk mode cases** | **CONFIRMED — temuan alat P2, tambah ke handoff writer sebagai W7** |
| **MULTI_CENT ×4 COUNTEREXAMPLE** (A4 batas multi-penerimaan): dua penerimaan 1 unit @10,00, koreksi/invoice ke 10,005 (dan turun) → persediaan 0,01 pada qty 0, WIP 20,01, AP −20,02; expected persediaan 0,00 / WIP 20,02 (jumlah sen per dokumen) | Nilai dibaca dari JSON GPT; konsisten dengan batas yang diakui writer ("≤0,01 per penerimaan"). Oracle GPT (M:3818/3820: tidak ada toleransi per penerimaan yang disahkan) sama dengan pembacaan Fable. Kelas: **CP6-03 residu multi-penerimaan CONFIRMED natively** — bukan lagi sekadar "batas diakui"; nilai persediaan ≠ 0 pada qty 0 melanggar M:3820. Prioritas P2 (nilai kecil, tapi kontrak eksplisit); butuh perbaikan writer atau pengecualian tertulis owner | CONFIRMED (dari JSON GPT; Fable belum menjalankan ulang sendiri) |
| XA6 EDIT_AFTER_EXPOSED_PREPARE → GPT INCOMPLETE "editability unproven" (Fable: PASS "penolakan eksplisit, tidak ada posting stale") | Perbedaan oracle, bukan fakta: fakta sama (SAVE_FILE ditolak setelah prepare via RPC). GPT membaca M:3817 "editable" sebagai kewajiban positif yang belum terbukti lewat aplikasi; Fable membaca jalur aplikasi (FINALIZE = prepare+post atomik) tidak pernah menyisakan draf prepared untuk diedit, jadi kewajiban itu tidak punya permukaan UI. Kedua bacaan sepakat: tidak ada posting stale; residu = A8 (grant RPC). Tidak menahan gate | NOTED (perbedaan interpretasi dicatat, tidak ada temuan produk baru) |
| Cross-review T3/rollback/CodeQL dari log (GPT): T3 25 berkas, pins reproduced, drill RESTORED_SAME_MEANING (bukan byte-identik; 19 error pg_cron; 29 constraint/1 index/6 view reparse varchar), rollback 127/127, CodeQL 0 di 4 bahasa | Sesuai bacaan Fable atas job install/browser 36095715362 dan rollback 36095723676 (§2 `out/fable_r8_results.md`); CodeQL tidak dibaca ulang Fable putaran ini | CONFIRMED (T3/rollback), NOTED (CodeQL) |
| C6: R8-C6-01 P2 (crosswalk 39 ACC / 24 LAU-R / 36 LAU-T belum ada; inventaris CR-MASUK belum diverifikasi) | Sejalan dengan `out/fable_c6_annex_review.md` (ACC-04/LAU-05 salah label; 7 sub-keputusan tanpa baris). GPT menambah syarat crosswalk ID asli — Fable setuju: itu bagian dari aturan D06 butir 3 | CONFIRMED |

## Putaran 2 (commit GPT a285aa1, 4b324cb) — 2026-09-25T05:46:41Z
| Klaim GPT | Verifikasi Fable | Label |
|---|---|---|
| Checkpoint run 36099496005 / job 107958771209 (audit 71b62c2): 1 native (retry ADJUSTMENT_DATE dengan grant dipulihkan) + 3 HTTP Auth nyata (matriks argumen valid, revocation, jangkauan prepare RPC via PostgREST) + 12 browser WIB (Potongan/Bagi Potongan/BS × 4 zona) | Skenario dibaca (`browser_round8/*`, hash beku di `gpt_browser_oracle_freeze.md`): oracle browser = M:3820/CP6-01 (semua zona menyimpan kemarin 00:30+07 dan payload RPC sama), independen dari test writer; HTTP memakai `http.login` nyata dan argumen valid (memperbaiki batas H1 Fable yang memakai argumen dummy). Run masih berjalan saat dicek; hasil belum diverifikasi | NOTED (menunggu hasil) |
| Crosswalk 75 ID asli (39 ACC + 36 LAU) → semua UNVERIFIED sebagai checklist cakupan, bukan 75 tes | Struktur dibaca: setiap ID punya baris Master; tidak ada klaim PASS. Konsisten dengan aturan D06 butir 3 | CONFIRMED |
| **Perbedaan cross-review**: "promosi ACC-04 dan LAU-05 ke BASELINE secara menyeluruh (Fable) terlalu luas untuk M1697; penundaan menyeluruh writer terlalu luas untuk M1757 → pisahkan baris pada batas fitur (a: perilaku lama BASELINE; b: perluasan baru CR-MASUK/CR-TUNDA menurut inventaris sumber)" | Rekomendasi Fable sebenarnya "BASELINE untuk mekanisme/tracking + PENDING KEBIJAKAN untuk sub-butir nilai/tarif" — sudah parsial, tetapi formulasi GPT (ACC-04a/b, LAU-05 split existing vs new mechanics) lebih presisi dan lebih mudah disahkan owner. **Fable menerima formulasi split GPT.** Kedua auditor sepakat menolak CR-TUNDA menyeluruh writer. Tambahan GPT: inventaris sumber per CR (entrypoint publik + penyimpanan) wajib sebelum owner menyetujui scope — Fable setuju (sumber yang ditinjau GPT: AccessoryIssuePage, LaundryPage, BsResolutionPage, 20ap facade) | CONFIRMED dengan penyesuaian rekomendasi Fable (W1 diperbarui) |

## Putaran 3 (commit GPT 19caeb9) — 2026-09-25T05:48:11Z
| Klaim GPT | Verifikasi Fable (log job 107958771209 dibaca langsung) | Label |
|---|---|---|
| Retry C0 ADJUSTMENT_DATE dengan grant baca dipulihkan → PASS; total 25/25 C0 PASS (rev1 INCOMPLETE dipertahankan) | `AUDITOR_CASES_AFTER` PASS 1; identitas tool_head 9dd7bc2 / product_ref a095a9d; oracle bisnis tidak berubah (file beku 7c2c19b6… sama). Fable: 25/25 di bawah C0 kini terbukti natively oleh GPT; oracle Fable (dokumen) konvergen | CONFIRMED |
| HTTP Auth nyata dengan argumen valid: matriks 18 cek PASS; revocation PASS; jangkauan helper prepare: tiga fungsi private erp, `schema_usage_authenticated=false`, endpoint publik 404 → PASS | `AUDITOR_HTTP_AFTER` PASS 3; kasus `G8HTTP:PREPARED_HELPER_REACHABILITY` menunjukkan `schema_usage_authenticated=false` dan ketiga helper di skema `erp` (tidak terekspos PostgREST). Dampak: (1) H1 Fable (dua cek gagal karena argumen dummy) **digantikan** oleh matriks GPT — CP6-23/BLOCKER-02 HTTP tertutup pada a095a9d; (2) A8 diperjelas: grant EXECUTE `authenticated` pada `erp.prepare_migration_opening_balance` **tidak** terjangkau lewat jalur HTTP publik (perlu koneksi DB langsung) → residu tetap P3 opsional, tidak ada eksploit browser | CONFIRMED |
| Browser WIB: cutting 4/4 dan BS 4/4 PASS lintas Asia/Jakarta, UTC, Etc/GMT+12, Pacific/Kiritimati (input 2026-09-24T00:30 WIB → payload RPC dan DB 2026-09-23T17:30:00Z, HTTP 200, form nyata); pickup 4/4 INCOMPLETE karena locator `getByLabel('Mandor')` timeout (bukan produk) | `AUDITOR_BROWSER_AFTER` PASS 8 / INCOMPLETE 4; sampel Kiritimati cutting: expected_utc = payload = stored = 2026-09-23T17:30:00.000Z. Empat pickup INCOMPLETE dengan error locator Playwright (skenario GPT), bukan penolakan produk. 12 user Auth dibuat dan dihapus, console_errors 0, primary_unchanged. Dampak: **CP6-01/A2 naik dari CLOSED_BY_SOURCE_REVIEW ke CLOSED natively (browser nyata) untuk Potongan dan BS**; Pickup menunggu rerun locator GPT | CONFIRMED (cutting, BS); NOTED (pickup pending) |
| Observasi marker laporan: C0 §3.4 menyebut laporan menandai `changed_since_filing` setelah koreksi periode tertutup; dua kasus AO tertutup memberi `false`; sumber `cp6_aw_t1_family.sql:836` mendefinisikan flag = filed && readiness ≠ READY (bukan digest perubahan isi) | Baris 836 diverifikasi Fable: `'changed_since_filing', v_filing is not null and v_readiness->>'status'<>'READY'`. Ini pertanyaan semantik kontrak-vs-implementasi (marker "berubah sejak filing" vs "belum READY"): tidak ada nilai uang/tanggal yang salah. **Ditambahkan sebagai W9 (pertanyaan ke writer/owner, P3)**; gate laporan tetap UNVERIFIED sesuai GPT | CONFIRMED (fakta), NOTED (klasifikasi) |
| A7 tetap P3 opsional; A8 tetap residu legacy/private, tidak dipromosikan jadi eksploit browser | Sejalan dengan keputusan Fable (§6 `out/fable_r8_results.md`) | CONFIRMED |

## Putaran 4 (commit GPT 07a384a, 9dbc1d5) — 2026-09-25T05:54:44Z
| Klaim GPT | Verifikasi Fable (log job 107960458342 dibaca langsung) | Label |
|---|---|---|
| Retry pickup-only (selector `.cpick-setup`, oracle/fixture tidak berubah, sha browser b2a93bec…): run 36100064157 → 4/4 PASS lintas 4 zona; input 2026-09-24T00:30 WIB → payload dan DB 2026-09-23T17:30:00.000Z | `AUDITOR_BROWSER_AFTER` PASS 4/4, identitas tool 9dd7bc2 / produk a095a9d, Auth pulih, primary_unchanged. Bersama run 36099496005 (Potongan 4/4, BS 4/4): **matriks browser WIB 12/12 lengkap → CP6-01/A2 tertutup natively di tiga halaman** | CONFIRMED |
| Sisa terbuka menurut GPT: CP6-03 residu multi-penerimaan (P2), R8-B1-01 (P2 alat), C6/D06/GATE-16, cakupan kontrak penuh, semantik marker laporan | Sama dengan daftar Fable (W1, W2, W7, W8, W9, O2, cakupan ALL) | CONFIRMED |

## Putaran 5 (commit GPT 17c2add) — 2026-09-25T05:58:36Z
| Klaim GPT | Verifikasi Fable | Label |
|---|---|---|
| Log penuh T2 run Fable 36095707100 dibaca GPT: identitas 230/31/65 tidak berubah, 12 HOLD identik, NEW 25/8/1; **AR 146 sekuensial + 28 race PASS** (angka 106 di dokumen Fable berasal dari ekor log 300 baris yang terpotong) | Fable tidak membaca ulang log AR penuh; angka 146+28 sama dengan run referensi 36037873682 yang Fable baca sendiri pada 9add57e. Koreksi diterima; `out/fable_r8_results.md` §2 diperbaiki | CONFIRMED (REUSED_GPT_LOG_READ, konsisten dengan referensi) |
| Selector-101 rerun Fable 36096194323 dicek silang GPT: 102 lookup, 102 claimable, sumber lama terpilih, tanpa cap 100 | Sama dengan bacaan Fable (xaudit_2 rev2 PASS) | CONFIRMED |
| CodeQL run 36090824553 memindai d113bed, bukan 9dd7bc2; satu diff file eksekusi sesudahnya (port preview browser 4177→4176 di host browser auditor); 0 hasil di 4 bahasa | Fable tidak memindai ulang; catatan scope diterima: CodeQL 0-hasil berlaku untuk d113bed, dan diff ke 9dd7bc2 hanya alat auditor (bukan produk) — konsisten dengan diff produk a095a9d..9dd7bc2 kosong yang Fable verifikasi | NOTED |
| Bukti T2 fixture summary (QUIETED/PAYROLL_APPROVED) tidak membuktikan kasus readiness negatif | Setuju; tidak ada klaim Fable yang bergantung padanya | NOTED |

## Putaran 6 — konsolidasi akhir GPT (commit f69b75c) — 2026-09-25T06:07:56Z
| Klaim GPT | Verifikasi Fable | Label |
|---|---|---|
| Konsolidasi selesai: bagian "terkini" di `AUDIT_REPORT_CP6.md`, `AUDIT_WRITER_HANDOFF_CP6.md` (W1–W9), `AUDIT_HANDOFF_CP6.md`, `AUDIT_PROGRESS.md`, index `gpt_round8`; "tidak ada run GPT yang masih ditunggu" | Semua bagian Fable (banner putaran 8, `fable_round8`, `fable_crossreview_gpt_r8`, `owner_decision_preparation`, `candidate_r8`, `status_r8` per temuan) dipertahankan. Penomoran W1–W9 sama dengan milik Fable; tidak ada temuan duplikat | CONFIRMED |
| Putusan: CP6 HOLD, audit_complete=false, production_go=false; gate GPT 4 HOLD / 6 UNVERIFIED / 0 ACCEPT; GATE-16/D06 HOLD terpisah | Sama dengan putusan Fable. Temuan aktif terdeduplikasi (CP6-03/W8 P2, R8-B1-01/W7 P2, R8-C6-01/W1 P2, W9 P3, CP6-05/06 lokal lama) sesuai register Fable | CONFIRMED |
| Tabel "sudah ditutup": CP6-01 12/12 browser; CP6-02/07/09/18 VERIFIED_INDEPENDENT untuk kasus tercatat; CP6-04 dua selector; CP6-24 race; C0 25/25; HTTP matriks; T3 subgate teknis diterima dengan batas | Setiap baris cocok dengan bukti yang Fable baca sendiri dari log (putaran 1–5 di atas) atau dari run Fable sendiri | CONFIRMED |
| CP6-19 `status_r8` diubah GPT menjadi "NO_STALE_POST_PROVED_ON_LEGIT_APP_PATH_LEGACY_EDITABILITY_UNVERIFIED" | Konsisten dengan perbedaan interpretasi yang dicatat di putaran 1; Fable menerima rumusan itu (fakta sama, residu = A8/W3) | CONFIRMED |

**Kesimpulan audit silang putaran 8 (Fable):** tidak ada klaim GPT yang REFUTED. Dua koreksi GPT atas Fable diterima (angka AR terpotong; formulasi split C6). Dua temuan GPT yang tidak ada di Fable dikonfirmasi dari log/sumber dan masuk handoff (R8-B1-01/W7, MULTI_CENT/W8). Dua auditor konvergen pada putusan dan daftar tugas W1–W9 / O2. Audit silang putaran 8 **selesai**; pekerjaan berikutnya menunggu writer (W1, W2, W7, W8, W9) dan owner (O2), sementara auditor dapat melanjutkan cakupan ALL/CP6-05/06 secara mandiri.

## Putaran 7 — GPT melanjutkan celah cakupan setelah konsolidasi (commit 18d6146 … cf01a09) — 2026-09-25T06:27:23Z
| Klaim GPT | Verifikasi Fable | Label |
|---|---|---|
| **CP6-05 dikonfirmasi di browser nyata** (run 36102303107 / job 107967209608, audit 6156adb): Pola dan duplikasi role — simpan pertama commit HTTP 200, balasan dijatuhkan di transport, klik Simpan lagi tanpa mengubah form → UUID request BARU (role: juga kode CUSTOM baru) → 409 unik, DB tetap satu baris. Tidak ada baris ganda, eskalasi hak, atau kerugian finansial. Oracle M:1679/M:3819 (identitas request dipertahankan; commit ambigu = menunggu sinkronisasi, bukan request baru). Run sebelumnya 36101907250 NOT_RUN (salah path file browser, dipertahankan) | Log job 107967209608 dibaca langsung: identitas 9dd7bc2/a095a9d, 2 COUNTEREXAMPLE + 2 INCOMPLETE, Auth pulih, primary_unchanged. **Sumber diverifikasi Fable**: `src/PatternPage.tsx:97,112` dan `src/AccessControlPage.tsx:202,220,238,253` memanggil `globalThis.crypto.randomUUID()` di dalam pemanggilan RPC (UUID baru tiap klik). **Survei Fable atas semua pemanggil `p_client_request_id` di `src/`**: seluruh halaman CONNECTED bisnis (potong, pickup, BS, WIP status, pocket, impor awal, aksesori, laundry/QC, pattern picker) memakai `envelope.id` yang dipertahankan — sesuai M:3819. Cacat terbatas pada dua halaman master data (Pola, Role), yang dilindungi keunikan DB. Fable: CONFIRMED, cakupan sempit; prioritas **P3** menurut Fable (tidak ada jalur finansial; GPT menulis P2 — perbedaan prioritas dicatat, owner/writer memutuskan) | CONFIRMED (fakta + sumber); prioritas berbeda P2 (GPT) vs P3 (Fable) |
| CP6-06 (unknown vs 0): dua kasus Laundry/QC INCOMPLETE — RPC Auth nyata mengembalikan 20/10, tetapi assertion render/refetch skenario GPT tetap membaca KPI "0" dan melempar sebelum mencatat detail | Log: kedua kasus gagal pada `expect(locator).toHaveText("20"/"10")` menerima "0" — ini kegagalan skenario/diagnostik GPT (belum bisa dibedakan antara UI menampilkan 0 sebagai "unknown" vs KPI belum termuat); GPT sendiri tidak mempromosikannya ke temuan. Sesuai | NOTED (INCOMPLETE, bukan bukti) |
| Binding ALL 22 state / 6 keluarga (`out/gpt_all_round8_binding.md`): sembilan blob sumber asli tidak berubah di 9dd7bc2; bukti parsial untuk P01/A01/W02/W03/C01; semua baris penuh UNVERIFIED; tidak ada run ALL baru diklaim | Dibaca: konsisten dengan status Fable CP6-17 `CONTRACT_DECIDED_COVERAGE_OPEN`; GPT memberi daftar kasus konkret per state — berguna sebagai rencana native berikutnya. Catatan GPT "BA dapat mengganti fungsi runtime, teks migrasi tidak berubah ≠ identitas runtime" benar | CONFIRMED |
| Tugas baru W10 (writer): pertahankan UUID/payload/expected-version yang tertunda selama ambigu dan rekonsiliasi hasil asli (M:1679/3819) untuk Pola dan Role | Diterima; ditambahkan ke handoff dengan cakupan sumber Fable (6 lokasi di 2 file) dan rujukan pola yang benar (`envelope.id` pada halaman CONNECTED) | CONFIRMED |

## Putaran 8 — pra-diagnosis Fable atas kasus "unknown" GPT (rev2 run 36102938451), dari sumber saja — 2026-09-25T06:40:53Z
Tidak ada run Fable; tidak menyentuh skenario GPT. Bahan: diagnostik yang GPT simpan (`out/gpt_unknown_run_36102938451.json`) + sumber kandidat a095a9d.
| Fakta | Sumber | Arti |
|---|---|---|
| Setelah gangguan dihilangkan, klik "Muat ulang data" → RPC `erp_get_laundry_qc_workspace_v1` HTTP 200 dengan body lengkap (keys scope/lookups/readiness/…; readiness semua true, lineage_issue_count 0), tetapi UI tetap KPI 0 dan banner **"Layanan UAT belum dapat dihubungi. Coba lagi setelah status diperiksa."** | JSON GPT `refetch_http_status`, `refetch_readiness`, `refetch_error_ui` | Bukan gangguan jaringan: respons diterima browser |
| `useLaundryQcWorkspace.load()`: setelah `client.rpc` sukses → `parseLaundryQcWorkspace(data)` → cek scope → `setWorkspace(parsed)`; **setiap exception** di jalur itu masuk `catch` → `setLoadError(normalizeClientError(error).message)` | `src/useLaundryQcWorkspace.ts:37-50` | KPI 0 berarti `setWorkspace` tidak pernah tercapai → ada exception SETELAH 200 |
| `normalizeClientError` memetakan error apa pun yang tidak dikenal (termasuk `Error` dari parser) ke `BACKEND_UNAVAILABLE` "Layanan UAT belum dapat dihubungi" | `src/lib/clientError.ts:35-71` (fallback terakhir) | Pesan "belum dapat dihubungi" menyembunyikan penyebab asli (kemungkinan besar penolakan parser) |
| Kandidat penolakan parser pada data fixture GPT: (a) `assertStableMetadata` (merek/model/size/potongan) ; (b) aturan produk tumpang-tindih (`laundryQcModel.ts:626-648`) — dua produk yang tertangkap (CP6-E-…/warna CP6-E-… dan CP6-RACE-SKU/NAVY, merek+model+size sama, warna beda) **tidak** memicu aturan ini; (c) `readiness.laundry_writer_ready/qc_writer_ready` harus sama dengan `vendors>0 && wash_processes>0 && rate_versions>0` / `fg_locations>0 && products>0` (`:649-655`) — bila fixture GPT tidak menyediakan rate version/vendor/lokasi FG sedangkan server melapor ready=true, parser melempar "Flag kesiapan writer tidak sama…"; (d) invarian baris `parseDelivery/parseReadyBatch/parseQcQueue` | `src/laundryQcModel.ts` | Rev3 GPT (parser murni atas respons nyata) akan menunjukkan pesan asli; Fable memperkirakan (c) atau (d), bukan produk gagal refetch |
| Terpisah dari itu: pada bacaan awal yang gagal, UI merender empat KPI **0** dengan banner error dan tulis terkunci | `ConnectedLaundryPage.tsx:412-416` (`?? 0`), `ConnectedQcFinalPage.tsx` sejenis | Inilah CP6-06 (M:3825 "unknown ≠ 0"): sudah teramati di rev2 tanpa perlu kontrol positif; yang belum terbukti hanya "refetch memulihkan" |
**Rekomendasi Fable (tanpa menunggu rev3):** (1) untuk writer, calon **W11 (P3 UX)**: exception klien (parser/kontrak) tidak boleh ditampilkan sebagai "layanan tidak dapat dihubungi" — tampilkan pesan parser asli agar operator dan auditor tahu bedanya jaringan vs data; (2) CP6-06 dapat dinilai dari fakta rev2: KPI menampilkan 0 saat data unknown (banner + kunci tulis adalah mitigasi, bukan pemenuhan M:3825); (3) kontrol positif GPT kemungkinan gagal karena fixture vs invarian parser, bukan karena produk gagal refetch — jangan dipromosikan jadi cacat recovery sebelum rev3.

## Putaran 9 — penyebab kegagalan kontrol positif "unknown" ditemukan dari sumber (Fable, tanpa run) — 2026-09-25T06:47:49Z
**Rantai sebab (semua diverifikasi di kandidat a095a9d):**
1. Respons refetch berisi produk dengan `model_id = a2000000-0000-0000-0000-000000000001` (tertangkap GPT di `refetch_products`).
2. Frontend memvalidasi setiap ID dengan `uuidPattern = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i` (`src/laundryQcModel.ts:123`); nibble versi harus 1–8. Diuji Fable dengan Node memakai regex persis dari sumber: `a2000000-…-000000000001` **REJECTED**; size/brand/produk lain (`c8c10000-0000-4000-8000-…`, `d125102c-8397-4cfc-…`) OK.
3. `parseProduct` → `id(raw.model_id,'Model produk')` melempar `Error('Model produk bukan UUID valid.')` (`laundryQcModel.ts:151,224`).
4. `useLaundryQcWorkspace.load()` menangkap exception itu dan menampilkan `normalizeClientError(error).message`; karena `Error` biasa tidak cocok dengan kode mana pun, fallback = `BACKEND_UNAVAILABLE` "Layanan UAT belum dapat dihubungi. Coba lagi setelah status diperiksa." (`src/lib/clientError.ts:71`). `setWorkspace` tidak pernah dipanggil → KPI tetap 0.
5. Asal ID itu: seed uji `supabase/tests/cp3_r3_full_schema_seed.sql` (model `CP3R3-MODEL`, dipakai juga oleh tes cp4/cp5 rollback) — UUID buatan tangan dengan nibble versi 0, **bukan data produk** dan bukan hasil `gen_random_uuid()`.

**Kesimpulan:** kontrol positif GPT gagal karena data seed uji tidak lolos validator UUID frontend, bukan karena produk gagal memulihkan data setelah refetch. Prediksi untuk rev3 GPT (parser murni): pesan asli = "Model produk bukan UUID valid.".

**Dampak dan tindak lanjut:**
- Untuk GPT: pakai fixture produk/model yang dibuat lewat API (UUID v4) atau model seed lain yang valid; ulangi kontrol positif. Kalau UI lalu menampilkan 20/10, kontrol positif PASS dan CP6-06 tinggal soal KPI 0 saat unknown.
- Untuk writer, **W11 (P3 UX, baru)**: exception klien (parser/kontrak) ditampilkan sebagai "layanan tidak dapat dihubungi" — tampilkan pesan asli (mis. kode `CLIENT_CONTRACT_MISMATCH`) agar jaringan vs data bisa dibedakan oleh operator/auditor. Bukti: `clientError.ts:71` fallback + kasus ini.
- Untuk writer/T3, **pertanyaan W12 (P3)**: pastikan tidak ada UUID non-standar (nibble versi 0 atau varian non-RFC) pada data hosted/legacy untuk tabel yang dibaca facade CP6 (products/models/sizes/brands/…); satu baris seperti itu membuat halaman Laundry/QC tidak dapat memuat sama sekali dengan pesan yang menyesatkan. Cek ini boleh masuk drill T3 (bukan tugas auditor pada hosted).
- CP6-06 (M:3825 unknown ≠ 0): fakta rev2 tetap — saat data belum dimuat, KPI merender 0 (`ConnectedLaundryPage.tsx:412-416`, QC sejenis) dengan banner error dan tulis terkunci. Penilaian akhir menunggu kontrol positif yang bersih.

## Putaran 10 — kasus unknown rev4/rev5 GPT (commit aa26d9f … a5a2d6e) — 2026-09-25T07:17:58Z
| Klaim GPT | Verifikasi Fable (log job 107979461548 dibaca langsung) | Label |
|---|---|---|
| Rev3 parser murni: "ID Mandor bukan UUID valid." (Laundry) / "ID model QC bukan UUID valid." (QC) | Sama persis dengan prediksi Fable putaran 9 (regex `laundryQcModel.ts:123` menolak seed `a1000000…`/`a2000000…`; pesan disamarkan `clientError.ts:71`) | CONFIRMED (konvergen) |
| Rev4 (run 36105734698): 4 INCOMPLETE karena fixture memanggil fungsi private `erp.set_contractor_hpp_policy_v1` tanpa grant — kesalahan fixture GPT | Dicatat; bukan produk. Rev5 memakai facade publik `public.erp_set_contractor_hpp_policy_v1` | NOTED |
| **Rev5 (run 36106291785, job 107979461548, audit a4b65b9)**: Laundry healthy control **PASS** (RPC 200, parser asli menerima, UI menampilkan 20; produk seed disisihkan `is_portal_visible=false` di salinan, dicatat); Laundry initial-read **COUNTEREXAMPLE** (read awal diputus → empat KPI menampilkan **0** dengan banner error dan tulis terkunci; gangguan dilepas → HTTP 200, UI 20, error hilang) → **CP6-06 terkonfirmasi native pada Laundry (M:3825 unknown ≠ 0)**; QC 2 INCOMPLETE (nama merek helper "Cutover brand" bertabrakan dengan fixture Laundry di DB yang sama → setup ditolak guard unik) | Log: identitas 9dd7bc2/a095a9d; `AUDITOR_BROWSER_AFTER` PASS 1 / COUNTEREXAMPLE 1 / INCOMPLETE 2; KPI awal "0" ×4 tercatat; refetch 20; Auth pulih; primary_unchanged. Klasifikasi Fable: CP6-06 **CONFIRMED** dengan cakupan Laundry; refetch memulihkan data (tidak ada bug recovery). Mitigasi (banner + kunci tulis) nyata tetapi angka 0 tetap dirender sebagai nilai di KPI. Prioritas: P2 per teks M:3825 (GPT); Fable setuju P2 untuk KPI/laporan, karena "0" adalah angka yang bisa dibaca sebagai fakta oleh operator | CONFIRMED |
| Rev6: hanya dua kasus QC dengan nama merek unik via SAVE_FILE; Laundry tidak diulang | Sesuai; menunggu hasil | NOTED |
**Dampak register:** CP6-06 → CONFIRMED_NATIVE_BROWSER (Laundry) P2 → **W13** writer: saat workspace belum termuat/gagal, KPI harus merender status "belum diketahui" (mis. "—"/"tidak tersedia"), bukan 0 (`ConnectedLaundryPage.tsx:412-416` `?? 0`, `totalReadyToSend(...) : 0`; QC serupa `ConnectedQcFinalPage.tsx:314-335`). Banner + kunci tulis dipertahankan.

## Status GPT (dari `out/gpt_phase2_review_20260925.md` LANGKAH BERIKUTNYA)
1. Rerun hanya kasus ADJUSTMENT_DATE setelah akses baca diperbaiki. 2. Fase native browser/timezone/HTTP sendiri termasuk A8 reachability. 3. Konsolidasi residu CP6-03 dan B1 ke handoff writer; C6 crosswalk; rekomendasi A7/A8.
GPT menyatakan konsolidasi selesai (f69b75c) tetapi **melanjutkan celah cakupan** (CP6-05/06 browser, binding ALL). Fable mengikuti lagi. Rencana GPT berikutnya: retry diagnostik 2 kasus unknown, lalu pembaruan laporan/index. Fable mengikuti commit berikutnya dan memperbarui dokumen ini.

## Dampak ke register (Fable)
- Baru **W7 (tool, P2)**: perluas grup ketat B1 ke mode race dan HTTP (kosakata status, ID ganda, cek bocor) — `scripts/cp6_auditor_modes.py run_races/run_http/finish`.
- **CP6-03 residu multi-penerimaan**: dari "batas diakui" menjadi CONFIRMED natively (GPT run 36097284096 MULTI_CENT ×4). Handoff A4 diperbarui: perbaikan (alokasi sen per dokumen sehingga qty 0 ⇒ nilai 0) atau pengecualian tertulis owner.
- 25 T2 beku: dua oracle independen (Fable dokumen, GPT native) konvergen 24/25 PASS di bawah C0; ADJUSTMENT_DATE INCOMPLETE. Harness writer masih perlu grup oracle baru (W2).
