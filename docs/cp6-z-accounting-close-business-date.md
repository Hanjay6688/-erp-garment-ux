# CP6 Z: canonical accounting-close day

Status: writer verification pending. Independent final audit pending. `production_go:false`.

Y at `bde9da786e3cde94cdb96e953310e5fac46f8574` can reject a lawful prior-day close when the authenticated caller's timezone differs from Jakarta. Native #176 and #177 reproduced the same case: Jakarta day 2026-09-14, target 2026-09-13, OWNER/authenticated session. Jakarta succeeds and persists the target; Etc/GMT+12 rejects with SQLSTATE P0001. The evidence is authenticated SQL with synthetic JWT claims, not a signed HTTP or UI reachability proof.

Z replaces one comparison in `erp.close_accounting_through(date,text)`: the caller-dependent `current_date` becomes `erp._cp3_business_date(current_timestamp)`. Owner/admin authorization, mandatory date and reason, monotonic close/reopen rules, checkpoint refresh, actor attribution, and audit insertion retain their exact prior definitions. The transaction-start timestamp semantics remain unchanged. No posted business row is rewritten by the migration.

The filename `20260914085912_erp_v2_6_20z_cp6_accounting_close_business_date.sql` was generated by the pinned Supabase CLI during native #177. Z is a new successor; admitted migration and rollback bytes through Y are immutable. The new capsule pins the predecessor and installed definitions, owner and ACL independently. Its pre-use boundary covers all 211 existing ERP tables except the migration ledger and the Z capsule itself. Unexpected history requires review; the migration does not repair it silently.

## Evidence qualification

Native #176 (`34823498503`, head `9a3ffb18f1d7370677afa777853cd303ced91b9f`) is **INCOMPLETE**: one qualified close defect, 15 controls and four harness errors. The harness called nonexistent `report_cash_balance(date)`; that is not an ERP defect.

Native #177 (`34824845423`, head `c978ea1c0d5a1185fca2c29d0db406777fed3aa5`) is **INCOMPLETE**: one qualified close defect, 18 controls and one harness error. The caller-date report control incorrectly requested `p_to > p_as_of`. The corrected audit uses the existing owner snapshot RPC, with the same income period ending at the earlier of the two balance-sheet dates. It compares `financial_position`, proves the ordinary 0.03 posting and READY status, and rolls back the full runtime boundary. Both failed artifacts are retained byte-for-byte with API identities and digests.

Required gates: 20 independent before/after cases with zero incomplete cases; ten native atomic admission refusals; source/owner/ACL and inherited trust verification; eight direct rollback guards; eleven inherited-object preflight faults; coherent capsule refusal before admission changes; 420 closed-admission schedules with 105 actual backend entries; all inherited Auth, native business, concurrency, UI-contract, build, ownership and cleanup gates. Full Z-to-Y restore must match 533 function definitions/owners/ACLs and all 213 pre-Z table snapshots. Counts here are requirements, not claimed execution results.

## No orphan code and review scope

The audit is invoked before and after Z in the native workflow. The new migration, runtime verifier, rollback guards, admission qualification, maintenance target, source gate and this document are all in the final source manifest. The matrix and five inherited race modules name Z as their active source. The rollback ladder restores Z to Y before invoking the historical Y-to-X controller. Historical helpers remain wired and unchanged except where their current source-generation checks advance to Z.

The new source gate is called by `npm run check:cp6`; its static result never substitutes for native proof. The final manifest binds both the failing Y oracle and the passing Z controls to the exact writer head. The generic reversal/long-transaction Jakarta-midnight hypothesis is **not executed or qualified** by this patch. It remains an adversarial follow-up target, alongside production completion/HPP cutoffs and material checkpoint boundaries. Do not claim exhaustive safety or CP7 readiness from this bounded closure.

Only `competition/cp6-j-closure-20260911` is writable. Main, PR24/25, hosted UAT and legacy stay outside this change. CP7 remains unstarted pending independent CP6 PASS and owner direction.

“Reliable data adalah dewa.” “Keuangan—termasuk laporan—stok, dan HPP adalah raja.”

“VENI. VIDI. VICI. ERP. — I CONQUERED ERP.”
