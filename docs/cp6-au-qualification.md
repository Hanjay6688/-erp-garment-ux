# AU independent review and writer qualification

CP6 remains HOLD. Production, hosted systems, main, deployment and CP7 are outside this change.

The independent review freezes AT at a8a8771cdab3bec28f04600c86870142270e8487. Run 35819787233 (auditor 320a0151b7fc12e34b21eecef8534722b5acf80c) reproduced four AS WIP defects, passed all sixteen AT temporal controls, and proved three blocked ordinary owner/admin master RPC paths. The display-name control passed. Rejected operations and every fixture restored their full database boundary.

AU changes six existing functions, extends one identity trigger to cover primary-key and predecessor writes, and introduces one private, empty-at-rest mutation context table. The controlled RPC authorizes exactly one product identity old/new pair, bound to backend, transaction and product. The trigger consumes that authority; direct table edits remain blocked, including a forged custom setting. Metadata/delete guards no longer trust the caller-controlled setting. Every installed foreign key to products counts as a reference for the unused-product decision. Used versions retain identity; a successor closes only the tail boundary and cannot cut underneath a recorded production timestamp. Identity validation holds a row share lock until transaction end. Exact explicit edit and cancellation retries return the original result without extra audit writes.

The native catalog capture (35820536608, head 553c72b7142ce296d077711310602cb9c5cb857e) is a rolled-back development observation, not installed-product qualification. Its original full JSON and artifact are preserved. The compact schema record binds the raw hash, native artifact, exact AT catalog/function digests, all eight new table objects and six changed function identities. The final model re-derives function definitions from current source; it imports only the unchanged native table shape. A retry query column spelling was corrected after the capture and before the permanent migration.

The generated migration requires closed, drained admission and the exact AT predecessor. Catalog, owners, ACLs, all historical capsules, migration statements and business data remain pinned. Installation requires the new context table to be empty. Rollback is pre-use only, restores all six original functions, removes only AU objects/markers and verifies the exact AT boundary. It refuses any post-use rollback.

Qualification plans: original 500 cases with unchanged historical statuses; 34 AS cases; 12 calendar-policy checks; 16 AT temporal controls; four AT status/output races; fifteen AU master/authority/history controls; six real ordinary master/output/validator races; ten real Auth/browser observations; two seeded pre-use recovery cycles and post-use refusal. These are planned until the exact-head native run passes. Own writer evidence is not independent acceptance. The existing master RPCs are tested under ordinary authenticated owner/admin claims; no new master UI path is claimed.

All old probes, case oracles, guards, migrations and rejected hypotheses remain unchanged. The historical AC-only workflow is expected to refuse successor heads and must not be weakened to make a badge green. The next reviewer must inspect the six-function delta, mutation-table ACLs, reference classification, cutoff semantics, retries and lock ordering independently.

The first installed qualification (9315a0b, run 35821214187) stopped with 13/14 AU cases passing and one invalid fixture. It tried to assign a price directly to a successor, which the unchanged economics-root guard correctly rejected. This is not a product defect. Its original failure artifact is retained; the replacement cancellation test uses a valid historical successor with an actual stock reference. No economics guard was changed.

Additional frozen-AT review 35821442087 (auditor 47d007c749e3bec49c16f27a443e5973e2823cf4) confirmed two further bypasses using ordinary authenticated statements: direct successor ID UPDATE, and INSERT with controlled version metadata after setting app.product_identity_controlled=on. Both changed database state when they should refuse. Every fixture and the AN primary were restored. AU closes the missing trigger events and removes setting-based authority.

Concurrency follows the existing pocket-period fail-fast contract. A losing simultaneous request must return POCKET_PERIOD_BUSY without changing committed data, then succeed after the winner commits or aborts. Static review found that the initial AU race harness demanded blocking despite that contract; the corrected harness requires explicit refusal-plus-retry observations. The legacy identity validator takes the same period gate before its product share lock, preventing a master from holding FG while waiting for that reader. Two dedicated validator/master schedules cover this lock-order boundary.

Run 35821734735 at 3091c5b passed all fifteen AU controls, sixteen AT controls and four AT races, then stopped before meaningful AU race observations: the first case lacked the fixture-only authenticated ERP schema USAGE grant and later committed fixtures reused the generic Cutover brand name. The fixes give each synthetic brand a unique name and grant schema USAGE only during these isolated ordinary-role calls, restoring the original ACL before full catalog verification. No production schema privilege or master UI endpoint was added. The two frozen-AT probes use the same explicit temporary schema grant.

Run 35822117787 at e150a30 passed fifteen AU controls and all four ordinary master/WIP fail-fast/retry schedules. Two private-validator probes were initially called under a role that has no EXECUTE grant. The same run also exposed that GRANT then REVOKE does not restore a formerly NULL schema ACL byte-for-byte. Qualification remained failed. The corrected harness runs private validators in their existing privileged writer context with owner claims, labels those two observations separately, and runs all six master races on a second disposable database copy. Only that copy gets authenticated schema USAGE; its exact single catalog delta and unchanged function owners/ACLs are checked. The parent AU catalog/data never receive the grant, remain exact, and the auxiliary copy is deleted.
