# CP6 AI work source family — qualification pending

Keep CP6 and production_go:false. Only competition/cp6-j-closure-20260911 is
writable, fast-forward only, one active writer. Check the live branch before
writing and pushing; stop on another writer's movement. Main, PR24/25, hosted
databases, merge, deployment and CP7 remain outside this wave.

Original AH is `965ceac45ef15ddc19c178d87a3606adffcf53a6`, tree
`183a0c2af8ba201e3754e9db77fe47501c196f4d`. Original writer Native35070855729
and CodeQL35070855668 passed. Artifact10436024116 SHA256
`3f05dead84a0e7bb67a69230c94e9677e3728152ca1f01cddbf8aa355f3195d6` was independently
downloaded, checked against its exact HEAD/tree and CRC-validated.

Independent original-AH run35076310148, harness4337b2f4ede0a24a54a925eb8a789eb37f8362a4,
proved one material work-source defect with five passing controls. A permitted
OWNER draft header changed from PO A to PO B, including its cutting group.
The child retained A's committed rate1.25; posting created a12.50 work accrual
on B, whose separately qualified ordinary control accrued25.00 at rate2.50.
Both ten-piece work drafts were valid before the change. The report remained
READY. No posted row or installed function was changed to create the defect.
Artifact10438516795 SHA256
`3a020902f2e03798b698eb6729358ef9d875474d1a6b72b29ba76668ef887a1a` preserves this.

That first run also has six INCOMPLETE return controls: the proposed fixture
used the same allocation twice in one return, forbidden by the existing
uq_sales_return_item_allocation constraint. This is a fixture error, not six
business defects. The corrected independent controls use two actual sale
allocations: sale120, refund40, remaining80, and receipt2pcs costing2.50.
The original failed observations are preserved. The corrected original AH
run must prove the same work defect and complete all eleven passing controls
before qualifying AI; expectations have not been weakened.

AI changes three existing functions. Work-line validation locks the current
parent and committed snapshot and fails closed on missing/mismatched source.
The existing posting-consistency guard rechecks the current PO, contractor,
component and rate after the header is locked. A valid draft may change source,
but its lines must be rebuilt for that source before it can post. Normal zero
and partial wages, an edit-and-restore, and a correctly rebuilt draft stay valid.
The main financial report checks the same posted relationship. Existing invalid
posted history refuses installation; no historical record is rewritten.

All callers use the native validator and posting trigger. Direct draft writes
are reproduced; post_work_completion, work quantities, payroll eligibility,
sewing terminal, WIP/HPP and reports are reviewed as consumers. The existing
used-snapshot guard refuses ordinary changes once a line references it. Its
definition and ACL are unchanged. Vendor invoices and returns revalidate their
source during posting and are covered by the retained AH controls. HTTP/UI/CSV
and complete role coverage remain BELUM TERUJI; a temporary schema USAGE fixture
does not establish deployed application reachability.

Planned native gate: original-AH 12 cases; 8 admission; 99 focused;
36 report controls; 142 combined business oracles; 16 two-session schedules
(8 work and8 unchanged AH return schedules); 20 maintenance schedules;
8 rollback refusal controls; exact533-function/223-table restoration to AH.
Synthetic report controls test the detector, not new ordinary business defects.
Read actual run/artifact status before claiming PASS; this document is a plan
until those gates finish. AI independent acceptance always remains pending
until the other chat checks the successor.

The historical500-case matrix is not automatically repeated. Unchanged SQL
through AH and unchanged business oracles retain exact source/runtime pins;
new work posting/locking/report paths are rerun as one relevant combined gate
after the focused cases stabilize. Every previous workflow body is preserved
under a strict qualified-source router. A green router is not native evidence.

Rollback restores the exact predecessor definitions, owners, ACLs,223 tables,
data and migration markers. The maintenance controller closes admission and
drains old sessions before repeating relation/source verification and rollback.
No hosted database is used.
