name: CP2 Free Local Restore Drill (Retired)

# Checkpoint 2 completed successfully in workflow run 33457299164,
# job 99699891972, at commit 41bda0f748ebef57155013ea1e2f3dc86602a85a.
#
# The executable one-time workflow was intentionally retired after:
# - encrypted archive validation,
# - matching local Supabase/Postgres 17.6.1.165 restore,
# - exact object/count/data reconciliation,
# - real Storage object round-trip verification,
# - local stack and plaintext cleanup.
#
# Durable non-secret evidence lives under:
# docs/evidence/2026-09-01/checkpoint_2_backup_manifest.json
# docs/evidence/2026-09-01/checkpoint_2_restore_proof.json
# docs/evidence/2026-09-01/checkpoint_2_backup_report.md
#
# Do not re-enable this file with historical recovery-artifact IDs.
# A future restore drill must create a fresh encrypted backup and a fresh,
# reviewed workflow on a new evidence checkpoint.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Checkpoint 2 restore drill is retired; use the persisted PASS evidence.'
