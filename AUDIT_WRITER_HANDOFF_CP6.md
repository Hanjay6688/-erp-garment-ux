# Handoff untuk WRITER — CP6 (disusun auditor Fable, 25 September 2026)

> ## Status putaran 8 (25 Sep 2026, head 9dd7bc2 / produk a095a9d) — ditambahkan auditor Fable
> Rincian: `out/fable_r8_results.md`. **Tertutup pada a095a9d (bukti auditor sendiri):** A1/CP6-09, A3/CP6-02, A4/CP6-03 (kasus satu penerimaan), A5/CP6-04, A6/CP6-24, A9/CP6-07 (D02), A10/CP6-18 (D03); A2/CP6-01 tertutup lewat review sumber **dan browser nyata GPT (run 36099496005: Potongan 4/4, BS 4/4; run 36100064157: Pickup 4/4 — 12/12 lintas 4 zona)**. HTTP: matriks argumen valid GPT 18 cek PASS menggantikan H1 Fable; helper prepare tidak terjangkau lewat HTTP publik. T3 package gate true, rollback AC..BA 127/127, T2 identik dengan referensi (25 beku tetap beku).
> **Yang masih untuk writer:** (W1, diperbarui setelah cross-review GPT) Lampiran C6: **pisahkan** ACC-04 → ACC-04a (perilaku lama: transfer/pemakaian internal/retur/settlement = BASELINE) dan ACC-04b (perluasan baru: pos servis, inspeksi, aksesori pulih, konversi = CR-MASUK/CR-TUNDA menurut inventaris sumber); LAU-05 → mekanisme vendor/proses/tarif/fisik-akuntansi lama = BASELINE vs komponen/paket/alokasi invoice baru = CR menurut inventaris; LAU-07 diganti LAU-DEC01–06 eksplisit; tambah baris ACC-DEC01/04/06/07, ERP-DEC02; sertakan **inventaris sumber per CR** (entrypoint publik + penyimpanan) dan crosswalk ke 75 ID asli (`out/gpt_c6_75_case_crosswalk.md`). Rujukan: `out/fable_c6_annex_review.md`, `out/gpt_c6_scope_amendment.md`. (W2) Harness T2: tambah grup oracle pasca-addendum dari `out/fable_t2_oracles_post_addendum.md` tanpa menghapus assertion beku; perbaiki fixture ADJUSTMENT_DATE per D01 §3.3. (W3) A8 direkomendasikan sebelum rilis (cabut grant `authenticated` pada `erp.prepare_migration_opening_balance`). (W4) Alat: mode SAMPLE_BROWSER sesekali INCOMPLETE `users_created 0` (run 36096186788) — flake; COMMIT pada koneksi kasus membuat grup crash `savepoint "auditor_case" does not exist` (rt_probe_2) — tandai rapi. (W5) Opsional A7. **(W7, P2 alat)** grup ketat B1 belum berlaku untuk mode race dan HTTP: status di luar kosakata diterima dan ID ganda menimpa hasil (`scripts/cp6_auditor_modes.py run_races/run_http/finish`; bukti GPT job 107952095197 dibaca Fable) — perluas `strict_group` ke kedua mode. **(W8, P2 produk)** CP6-03 residu multi-penerimaan CONFIRMED natively (GPT run 36097284096 MULTI_CENT ×4: dua penerimaan @10,00 → koreksi 10,005 → persediaan 0,01 pada qty 0, WIP 20,01 vs expected 20,02): sen per dokumen harus dialokasikan sehingga qty 0 ⇒ nilai 0 (M:3820), atau owner menulis pengecualian eksplisit. **(W9, P3 pertanyaan)** semantik `changed_since_filing` pada laporan owner: C0 §3.4 menyiratkan penanda "berubah sejak filing" setelah koreksi periode tertutup, implementasi `cp6_aw_t1_family.sql:836` = `filed && readiness<>READY` (dua kasus AO tertutup memberi false meski ada koreksi). Tidak ada nilai uang/tanggal salah; writer/owner tegaskan makna yang dimaksud atau ubah flag menjadi digest perubahan isi. (W6) Catatan A1: identitas CASH_BANK per `cash_account_id` (bukan COA) dan legacy-vs-legacy tidak dicek — konfirmasi apakah itu alias nyata.
> **Untuk owner:** (O1) **SELESAI** — owner mengonfirmasi langsung ke auditor (2026-09-25T05:29Z): teks addendum bagian 1–8 hash d39762da… sah untuk D01–D05 (OWNER_CONFIRMED_TO_AUDITOR). (O2) sahkan D06 hanya setelah W1 dan pencocokan ulang auditor. (O3) **SELESAI** — owner menegaskan D03 §5.3: boleh posting dengan provenance unknown; syarat: model PO+ukuran cocok, merek/warna yang terisi wajib cocok, yang kosong dicatat unknown (bukan cocok), dasar penetapan disimpan, WIP lama tidak ditolak hanya karena atribut belum lengkap. Perilaku kandidat sudah sesuai; writer cukup mencatat kalimat ini di addendum (C0) saat revisi C6.
> **Untuk auditor (berikutnya):** rerun C6 setelah W1; baca run T2 dengan grup oracle baru; skenario browser B4 sendiri; cakupan ALL 22 state/6 keluarga; A4 multi-penerimaan.
> Verdict tetap: **CP6 HOLD, audit_complete=false, production_go=false.** Penahan yang tersisa bukan lagi cacat produk P1, melainkan: 25 kasus T2 beku (perlu oracle baru dijalankan), D06/GATE-16 (C6), cakupan ALL, browser auditor, konfirmasi owner atas teks addendum.


Status: **CP6 HOLD · audit_complete=false · production_go=false.** Produk acuan `9add57ea8c2b6b2dc37c0134717d4d39ba30b5dc`; alat writer terakhir yang diuji auditor `d284e9b` (produk tidak berubah). Dokumen ini berisi daftar kerja writer yang **tidak menunggu keputusan owner**, lalu daftar yang **menunggu D01–D06** (`OWNER_DECISIONS_CP6_DRAFT.md`). Bukti: `out/fable_native15_xaudit5_results.md`, `out/verify_*.md`, `audit/CP6_COMBINED_INDEX.json`, `audit/runs_fable/*.json`. Oracle tetap dari tiga kontrak; auditor menulis skenario/oracle, writer memperbaiki produk dan alat. Jangan melonggarkan guard atau mengubah expected agar tes hijau (M:1769).

## A. Perbaikan produk yang wajib (tidak menunggu owner)
| # | ID | Prio | Yang salah (bukti) | Perbaikan yang diharapkan | Bukti penerimaan yang diminta auditor |
|---|---|---|---|---|---|
| A1 | CP6-09 (F1-12) | P1 | Batch impor kedua untuk item stok yang sama (material_sku+lokasi; product_sku+lokasi) di-FINALIZE lagi → stok/nilai awal dobel (MATERIAL_INVENTORY/FG_INVENTORY +15,75 dua kali; qty 7→14). Guard AR hanya cek impor vs legacy (`…20ar….sql:372` XOR migration_batch_id). Terjadi juga di dua sesi nyata (kunci ada, guard tidak). run 36044022037, 36045629594, 36065517737 R1. | Identitas item/dokumen lintas batch pada FINALIZE impor (dan `erp.post_opening_balance` untuk impor vs impor) untuk SEMUA jenis saldo: material non-roll, roll, FG (produk+gudang+grade), BS, WIP, pihak, kas. Penolakan persis + tidak ada perubahan ledger. Sumber berbeda (roll/gudang/produk lain, impor parsial yang sah) tetap boleh. | Rerun `audit/scenarios/open_1.py`, `open_2.py`, `xaudit_5.py` (R1) pada head baru: kasus overlap → PASS dengan pesan penolakan tercatat; kasus positif distinct-source (auditor tambahkan) → POSTED. |
| A2 | CP6-01 (F1-14) | P1 | `ConnectedCuttingPage.tsx:272`, `ConnectedPickupPage.tsx:200`, `ConnectedBsResolutionPage.tsx:36-40` memakai `new Date(v).toISOString()` (zona perangkat). RPC hanya cast `::timestamptz`. Helper WIB `cp6WibPhysicalTimeToIso` sudah ada (dipakai Laundry/QC); facade aksesori bahkan memaksa sufiks `+07:00` (`…20ap….sql:502-503`). | Ketiga halaman memakai helper WIB (termasuk `nowInput`); nilai tidak valid → null/tolak, bukan throw. Opsional: RPC potong/pickup/BS memvalidasi sufiks +07:00 seperti facade aksesori. | Tes unit matriks TZ (Asia/Jakarta, Asia/Makassar, UTC, Pacific/Kiritimati) yang gagal di kode lama dan lulus di kode baru; DOM test per halaman; `audit/tools/tzprobe.mjs` → `equal:true` di semua zona; idealnya satu kasus browser nyata. |
| A3 | CP6-02 (F1-16) | P1 | COMPLETE WIP saldo awal bertanggal sebelum pembalikan output sebelumnya dibukukan → saldo tahap −8 dan WIP GL −20 pada prefix (`cp6_az_t1_family.sql:817-818` sisa tanpa dimensi tanggal; `:850-853` hanya cutover/hari ini). run 36048357523, 36065350201 SI-02. | COMPLETE menolak `date` < tanggal fisik pembalikan output sebelumnya untuk sumber yang sama (atau model sisa per tanggal). Penolakan persis. | `xaudit_1.py` U02 dan `stock_import_scenario.py` SI-02 → PASS; kasus positif (tanggal ≥ pembalikan) → POSTED. |
| A4 | CP6-03 (F1-17) | P2 | Koreksi harga 1 unit yang habis dikonsumsi: `round(qty×Δ)` per gerakan (AZ:256-279) vs delta endpoint yang sudah dibulatkan (AP:3292-3314) → persediaan ±0,01 pada qty 0, WIP 10,02/10,00; sama di jalur invoice. run 36048357523, 36051514868, 36065350201 MONEY×4. | Selisih pembulatan mengikuti nilai yang benar-benar diposting per unit (sisa ke posisi barang saat ini), sehingga qty 0 ⇒ nilai 0 dan tidak ada persediaan negatif harian. Aturan pembulatan kanonik ditulis. | `xaudit_1.py` U03 (2), `xaudit_2.py` invoice (2), MONEY×4 → PASS kedua arah. |
| A5 | CP6-04 (F1-15) | P2 | Lookups BS Resolution (sumber laundry/penerimaan/klaim) `limit 100` tanpa paging/search (`…19_cp5_bs_resolution_recovery.sql:768-770, 799, 828`); selector draf impor `recent` = 50 (`erp_get_initial_import_workspace_v1`) tanpa search/paging. run 36051514868, 36066079063. | Saring claimable/DRAFT di SQL sebelum LIMIT dan sediakan pencarian/paging (atau pilih-by-ID) di UI untuk kedua selector; periksa selector lain (pocket, payroll, prepayment). | `xaudit_2.py` selector-101 dan `import_selector_fable_fix.py` → PASS (sumber tertua tetap terpilih). |
| A6 | CP6-24 | P3 | Close tanggal yang sama diulang → filing kedua (`cp6_aw_t1_family.sql:632` hanya menolak tanggal lebih awal). Tanpa dampak terbukti (pembaca mengabaikan filing degeneratif). | No-op/penolakan bila `p_closed_through = closed_through` saat ini, atau partial unique index. | `xaudit_5.py` R2 → tepat satu filing. |
| A7 | CP6-25 | P3 | COMPLETE bersamaan ditolak `POCKET_PERIOD_BUSY` bukan `STALE_VERSION` (aman). | Opsional: pesan versi usang saat kunci pocket sibuk. | — |
| A8 | CP6-19 (residu) | P3 opsional | Jalur aplikasi sah sudah benar (xaudit_6 rev3, run 36081137254, 2/2 PASS: SAVE_FILE menghapus item prepared DRAFT `…20ap….sql:1634-1636`; edit setelah prepare RPC ditolak eksplisit). Residu: `erp.prepare_migration_opening_balance` di-grant ke `authenticated` dan mengembalikan header lama tanpa cek item-vs-staging (`:2283`); hanya edit SQL langsung (SI-04) yang bisa menyimpangkannya. | Opsional: cabut grant `authenticated` pada prepare RPC (hanya FINALIZE yang memanggilnya) atau tambah cek item prepared = staging VALID saat dipanggil ulang. | `xaudit_6.py` tetap PASS; SI-04 → penolakan/rebuild. |
| A9 | CP6-07 (D02=A) | **P1** | Refund uang muka 100 bertanggal 21 Sep diterima padahal kapasitas as-of tanggal itu 67,25 (guard hanya kapasitas saat ini; run 36065350201 DATED_CAPACITY ×3 COUNTEREXAMPLE). Setelah D02 disahkan ini cacat produk, bukan lagi "as-of P2". | Penolakan persis bila melebihi kapasitas as-of pada tanggal transaksi dan prefix sesudahnya; berlaku uang muka supplier/customer/vendor dan stok (AUD-S04/B04). | DATED_CAPACITY ×3 → PASS; ORDERED_CONTROL ×3 tetap PASS. |
| A10 | CP6-18 (D03=A) | P2 | WIP awal dengan produk A/brand A/Blue diselesaikan sebagai produk B/Red (SI-01, run 36065350201; `cp6_az_t1_family.sql:855-862` tidak merujuk product_id item opening). Setelah D03 disahkan ini cacat produk. | Completion menolak produk ≠ item opening bila diisi; bila kosong, kecocokan fisik/sumber diperiksa dan produk hasil diberi provenance. | SI-01 → penolakan persis + kontrol positif. |

## B. Alat/protokol (tidak menunggu owner)
| # | ID | Yang salah | Perbaikan |
|---|---|---|---|
| B1 | CP6-10 (F1-04/R01) | Runner skenario: id kasus ganda menimpa hasil; status di luar kosakata jatuh ke PASS di grup; commit ke skema `public` lewat koneksi kedua tidak terdeteksi; advisory lock lolos. | Tolak id ganda; batasi kosakata status; deteksi commit `public`; cetak planned vs final. Verifikasi: `rt_probe_1.py`/`rt_probe_2.py` menghasilkan INCOMPLETE untuk kasus yang sengaja bocor. |
| B2 | CP6-11 (F1-18/O01) | Job install T3 hijau hanya dari jumlah stage (`cp6_t3_package_run.py:134,148`); drill/`primary_unchanged`/advisor tidak mempengaruhi. | Assert `backup_restore_drill`, `primary_unchanged`, status advisor; hapus komentar "green job can be cited without reading the log". |
| B3 | CP6-12 (F1-05/R02) | AW..AZ rollback sudah ada dan lulus siklus (run 36063754595, 22 PASS). AC..AV varian paket rilis NOT_BUILT; guard digest rollback AC hanya mengenal digest rantai uji (`7b5690a2…/7a3613d8…`), bukan berkas rilis (`871fb32b…`). Siklus 2 mengabaikan `captured_at`/`boundary_snapshot`; post-use hanya INSERT audit_logs. | Bangun rollback AC..AV varian rilis (digest ledger yang benar-benar dipasang applier), siklus penuh AZ→AC→AB, matriks post-use dengan ≥1 transaksi bisnis tiap keluarga. Jangan melonggarkan guard. |
| B4 | GATE-04/08 | Runtime auditor belum punya mode browser (browser→HTTP→runtime). | Mode browser di `cp6_auditor_modes.py` (dasar `cp6_t3_browser`), agar auditor menjalankan Playwright-nya sendiri terhadap stack sekali pakai (Auth nyata). |
| B5 | CP6-14 (F1-03) | Fixture T2 `QUIETED`/`PAYROLL_APPROVED` atas keputusan yang tidak di kontrak. | Cetak completion fixture di log per run; jangan menutup kasus negatif readiness yang diuji. |
| B6 | Bukti | — | Setiap run: head_sha, attempt, label (T1/T2/T3/AUDITOR); commit produk acuan dicatat terpisah dari head alat; hasil beku tidak dilabel ulang. |

## C. Keputusan owner D01–D06 — SUDAH DISAHKAN (25 Sep 2026, pilihan A semua) → tugas konkret writer
Sumber: konfirmasi langsung owner di sesi audit Fable 25 Sep 2026 ("Ya, semua sesuai usulan", termasuk akun lawan AX = pendapatan lain-lain). Rincian di `OWNER_DECISIONS_CP6_DRAFT.md` bagian "Pengesahan dan status". Tiga kontrak belum berubah: **C0** di bawah adalah prasyarat dokumen.
| ID | Keputusan | Tugas writer | Prioritas | Bukti penutup (auditor) |
|---|---|---|---|---|
| C0 | Semua | Tulis addendum bernomor (mis. `ERP_ADDENDUM_OWNER_DECISIONS_CP6_2026-09-25.md`) yang mengutip klausul M/P/BR yang diperjelas untuk D01–D06, minta pengesahan tertulis owner, simpan di paket kontrak. Sampai itu ada, label keputusan = OWNER_CONFIRMED_CHAT. | P1 (dokumen) | Auditor cek hash + isi vs konfirmasi chat. |
| C1 (D01) | Tanggal koreksi = posisi nyata barang | Kandidat sudah berperilaku demikian (AY/AZ). Tulis eksplisit aturan hari ADJUSTMENT_DATE untuk bahan, batas periode tertutup (receipt tertutup vs tahap terbuka), dan penyajian koreksi lintas periode. | P2 | Auditor menulis oracle untuk 8 AS + 12 kalender + 2 AO yang beku; rerun `cp6-t2-regression.yml`; hasil beku lama tetap tercatat. |
| C2 (D02) | Kapasitas per tanggal | **CP6-07 naik kembali menjadi cacat produk wajib (P1)**: refund/pemakaian/alokasi/koreksi bertanggal ditolak persis bila melebihi kapasitas as-of pada tanggalnya dan prefix sesudahnya, untuk uang muka supplier/customer/vendor (saldo awal, koreksi, reservasi, alokasi, refund, reversal) dan kelas stok AUD-S04/B04. Tidak ada perubahan ledger saat ditolak. | **P1** | `business_scenarios_reconstructed.py` DATED_CAPACITY ×3 → penolakan persis; ORDERED_CONTROL ×3 tetap PASS; AUD-S04/B04 rerun. |
| C3 (D03) | Produk WIP awal mengikat bila diisi | **CP6-18 menjadi cacat produk (P2)**: completion dengan produk ≠ item opening (bila diisi) ditolak persis (`cp6_az_t1_family.sql:855-862` harus merujuk product_id item opening); bila kosong, kecocokan PO/model/pola/ukuran/material/warna diperiksa dan produk hasil dicatat dengan provenance. | P2 | SI-01 → penolakan persis; kontrol positif: produk sama → POSTED; WIP tanpa produk + kecocokan sah → POSTED. |
| C4 (D04) | P-03 per tanggal terbukti | Per-date scoping hanya untuk cek yang lineage tanggalnya terbukti; sisanya tetap blok semua tanggal (perilaku sekarang). Dokumentasikan daftar cek mana yang per-tanggal. | P3 | Tidak ada temuan yang berubah; auditor cek daftar vs perilaku. |
| C5 (D05) | AX + akun lawan OTHER_INCOME disahkan | Tidak ada perubahan produk (fg_acc_1 sudah sesuai). Cantumkan akun lawan di addendum C0. | — | Sudah lulus (bagian D). |
| C6 (D06) | CR aksesori/laundry dipisah | Buat daftar acceptance ID per fitur: baseline CP6 / CR sudah masuk / CR ditunda / dependensi CP7. Auditor review, owner sahkan lewat addendum C0. | P2 (dokumen) | GATE-16 turun dari HOLD ke ACCEPT hanya setelah daftar disahkan dan baseline lulus. |

## D. Yang sudah lulus oracle auditor (tidak perlu dikerjakan ulang; jangan diregresi)
CP6-19 jalur aplikasi sah (edit draf setelah VALIDATE → FINALIZE memposting isi terakhir; edit setelah prepare RPC ditolak eksplisit; xaudit_6 rev3 run 36081137254). Tutup buku S06 atomik + penolakan persis + filing immutable; akses fail-closed 10 facade (lapisan DB dan HTTP dengan Auth nyata: anon/GUDANG/viewer/OWNER/revocation/unmapped); aksesori 7 PCS eceran persis M:94; FG tanpa sumber (konservasi, 9 penolakan, periode tertutup, nol eksplisit ditolak saat pembanding ada); replay UUID idempoten (impor, WIP_OUTPUT); edit identitas SKU sebelum/sesudah fakta stok baru; tumpang tindih jalur lama setelah impor ditolak 10/10; total kontrol tidak dibukukan; T2/T3/CodeQL direproduksi; AW..AZ rollback siklus (writer).

## E. Cara auditor memverifikasi ulang
1. Push head baru H (produk boleh berubah — ini kandidat baru). Auditor mencatat H, membandingkan diff, dan menjalankan ulang skenario yang terdampak lewat `cp6-auditor-scenario.yml` (`phase=after`) dengan berkas di `audit/scenarios/` (hash di `audit/scenarios/FABLE_SHA256SUMS` dan `native15_manifest.json`).
2. Setiap temuan ditutup hanya oleh run auditor (INDEPENDENT_NATIVE_RERUN) pada H dengan status PASS per kasus, bukan warna job dan bukan run writer.
3. D01–D06 sudah disahkan (25 Sep): auditor menulis oracle baru untuk 25 kasus T2 + CP6-07/18 setelah addendum C0 ada; writer mengimplementasikan A9/A10 + C1–C6; T2 dijalankan ulang; hasil beku lama tetap tercatat.

## F. Yang belum diperiksa auditor (batas)
Browser→HTTP→runtime UI; oracle same-source/distinct-source untuk CP6-09 (dibuat auditor); cabang AY rev6–7.4/AZ rev2.x di luar bentuk dasar; cakupan ALL 22 state/6 keluarga end-to-end; perilaku AV penuh (hanya 2 kontrol identitas); rollback AC..AV varian rilis; G-01 hosted.
